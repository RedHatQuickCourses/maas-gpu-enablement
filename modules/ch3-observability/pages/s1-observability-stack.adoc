:time_estimate: 22

= The MaaS Observability Stack

_Estimated reading time: *{time_estimate} minutes*._

Objective::

Understand how hardware telemetry flows from GPU accelerators to the platform observability plane. Learn the integration between OpenShift's built-in monitoring (Prometheus/Thanos) and standalone visualization using the Grafana Operator for correlating platform, node, and accelerator metrics.

== Why GPU Observability Matters for MaaS

At 3:14 AM on node-gpu-7, DCGM detected GPU memory at 38GB/40GB (95% utilization). Alert fired at 3:18 AM. The platform team drained the node before 4:00 AM production peak serving 200 requests/second inference traffic.

**Prevented:** $50,000 revenue loss from cascading service failures.

**Without monitoring:** 4:00 AM crash during peak demand, 45-minute recovery window, customer SLA breach, manual root cause investigation without historical data.

This incident demonstrates why comprehensive GPU observability is non-negotiable for production Models-as-a-Service platforms. Operating MaaS at scale requires comprehensive visibility into the entire infrastructure stack. For GPU-accelerated deployments, this means monitoring:

* **Infrastructure health**: CPU, memory, network, storage
* **GPU hardware health**: Temperature 78°C, power consumption 280W/300W, ECC error rates
* **GPU utilization**: Compute usage 85%, memory usage 32GB/40GB, MIG instance allocation
* **Workload performance**: Inference throughput, latency P99 45ms, batch sizes
* **Cost allocation**: Per-model GPU usage for chargeback and optimization

Without proper observability, organizations face:

* Unexpected hardware failures without early warning (average cost: $50K-$200K per incident)
* Suboptimal resource utilization and wasted capacity (typical: 40% idle GPUs undetected)
* Inability to correlate application performance with infrastructure metrics
* Lack of data for capacity planning and cost optimization
* Difficulty troubleshooting inference performance issues (45 minutes vs. 4 minutes with monitoring)

[TIP]
====
**Production GPU alert thresholds from NVIDIA deployments:**

* **Temperature**: 80°C warning, 85°C critical (GPU throttling begins at 87-90°C)
* **Memory**: 85% warning, 92% critical (OOM kills occur at 95%+)
* **Power**: 90% sustained indicates inadequate cooling or PSU capacity
* **ECC errors**: Any correctable errors warrant investigation; uncorrectable errors require immediate node drain

These thresholds provide 15-30 minute early warning before hardware failure or performance degradation.
====

== Observability Impact: Before and After

The difference between operating GPU infrastructure with and without comprehensive monitoring is dramatic. Consider a real incident: inference latency spikes to 2 seconds (normal: 45ms P99).

=== Without GPU Monitoring (45-minute resolution)

**Timeline:**

1. **T+0 min**: User reports slow inference responses via support ticket
2. **T+5 min**: Ticket escalated to platform team, engineer begins investigation
3. **T+8 min**: Check application logs (no GPU visibility), no obvious errors found
4. **T+13 min**: SSH to worker nodes, run `nvidia-smi` on 20 GPU nodes manually
5. **T+20 min**: Discover GPU 3 on node-gpu-12 at 100% utilization (memory leak)
6. **T+28 min**: Identify culprit pod by cross-referencing timestamps
7. **T+33 min**: Restart pod, performance returns to normal
8. **T+45 min**: Incident resolved, but **root cause unknown** (no historical data)

**Result:** Symptom fixed through manual investigation. No visibility into why the memory leak occurred, when it started, or whether it's happening elsewhere.

=== With DCGM Observability Stack (4-minute resolution)

**Timeline:**

1. **T+0 min**: Grafana alert fires: "GPU memory >92% on node-gpu-12 GPU 3"
2. **T+0.5 min**: Engineer opens DCGM dashboard, sees GPU memory climbing from 30GB → 39GB over 6 hours
3. **T+1 min**: Dashboard correlation panel shows pod `llama-7b-deployment-abc123` started 6.5 hours ago
4. **T+2 min**: Historical metrics reveal memory leak pattern: +1.5GB/hour sustained growth
5. **T+3 min**: Identify root cause: Missing memory cleanup in custom vLLM configuration
6. **T+4 min**: Drain node, fix configuration, redeploy with monitoring validation

**Result:** Root cause identified (configuration error), fix applied, monitoring confirms resolution. Historical data enables pattern detection across fleet.

=== The 11x Difference

[cols="2,2,2",options="header"]
|===
|Metric |Without Monitoring |With DCGM Stack

|**Time to Resolution**
|45 minutes
|4 minutes (11x faster)

|**Root Cause Analysis**
|❌ Unknown (symptom only)
|✅ Configuration error identified

|**Prevention**
|Manual inspection required
|Automated alerting + historical trends

|**Blast Radius**
|Single incident
|Fleet-wide pattern detection

|**Business Impact**
|$2,500 lost revenue (45 min downtime)
|$200 lost revenue (4 min downtime)
|===

This scenario repeats across GPU operations: thermal throttling detection, memory fragmentation, driver crashes, PCIe bandwidth degradation, and MIG instance scheduling conflicts. Comprehensive observability transforms reactive fire-fighting into proactive platform management.

== The Telemetry Flow

GPU metrics flow through a multi-tier observability stack:

=== Layer 1: GPU Hardware Telemetry

**NVIDIA DCGM (Data Center GPU Manager)** continuously collects telemetry directly from GPU hardware:

* GPU utilization percentage
* Memory utilization and allocation
* GPU temperature and power draw
* PCIe throughput
* ECC (error-correcting code) errors
* NVLink bandwidth (for multi-GPU systems)
* Per-process GPU usage
* MIG instance metrics

DCGM runs as a lightweight daemon on each GPU node with minimal performance overhead (<1% GPU utilization).

**Integration with Chapter 1:** Your xref:ch1-gpu-operator:s2-operators-overview.adoc[ClusterPolicy from Chapter 1] already enabled DCGM:

[source,yaml]
----
spec:
  dcgm:
    enabled: true  # <1>
----
<1> Deployed `nvidia-dcgm` DaemonSet automatically across all GPU nodes

**MIG-aware metrics:** DCGM tracks xref:ch2-mig:s1-mig-overview.adoc[MIG instances from Chapter 2] with `GPU_I_ID` labels, enabling per-instance monitoring:

* `GPU=0, GPU_I_ID=0` → First MIG instance (e.g., 1g.5gb profile)
* `GPU=0, GPU_I_ID=1` → Second MIG instance (e.g., 3g.20gb profile)
* `GPU=1, GPU_I_ID=0` → First instance on second physical GPU

This labeling enables per-MIG-instance billing, utilization tracking, and multi-tenant cost allocation for MaaS platforms.

**Verification:** Confirm DCGM is running on your cluster:

[source,bash]
----
$ oc get pods -n nvidia-gpu-operator -l app=nvidia-dcgm
NAME                  READY   STATUS    RESTARTS   AGE
nvidia-dcgm-xxxxx     1/1     Running   0          5d
nvidia-dcgm-yyyyy     1/1     Running   0          5d
----

You should see one DCGM pod per GPU node.

=== Layer 2: Metric Export

**DCGM Exporter** translates DCGM telemetry into Prometheus-compatible metrics format:

* Scrapes DCGM on a configurable interval (default: 30 seconds)
* Exposes metrics via HTTP endpoint on port 9400
* Adds Kubernetes labels (pod, namespace, node) for correlation
* Supports custom metric collection profiles

**ClusterPolicy configuration:** Your GPU Operator ClusterPolicy also enabled the DCGM Exporter:

[source,yaml]
----
spec:
  dcgmExporter:
    enabled: true           # <1>
    serviceMonitor:
      enabled: true         # <2>
      interval: 30s         # <3>
    config:
      name: ""              # <4>
----
<1> Deploys `nvidia-dcgm-exporter` DaemonSet on GPU nodes
<2> Auto-creates ServiceMonitor for Prometheus scraping (discovered when namespace is labeled)
<3> Scrape interval balances metric freshness vs overhead (default: 30 seconds)
<4> Empty string uses default metrics profile; custom ConfigMap enables selective metric collection

Example metrics exported:
```
DCGM_FI_DEV_GPU_UTIL{gpu="0", UUID="GPU-xxxxx", device="nvidia0"} 85
DCGM_FI_DEV_FB_USED{gpu="0", UUID="GPU-xxxxx", device="nvidia0"} 34359738368
DCGM_FI_DEV_POWER_USAGE{gpu="0", UUID="GPU-xxxxx", device="nvidia0"} 280
DCGM_FI_DEV_GPU_TEMP{gpu="0", UUID="GPU-xxxxx", device="nvidia0"} 78
DCGM_FI_DEV_SM_CLOCK{gpu="0", UUID="GPU-xxxxx", device="nvidia0"} 1410
```

**Hands-on verification:** Check that the DCGM Exporter is exposing metrics:

[source,bash]
----
$ EXPORTER_POD=$(oc get pods -n nvidia-gpu-operator -l app=nvidia-dcgm-exporter -o name | head -1)
$ oc exec -n nvidia-gpu-operator $EXPORTER_POD -- curl -s localhost:9400/metrics | grep DCGM_FI_DEV_GPU_UTIL
DCGM_FI_DEV_GPU_UTIL{gpu="0",UUID="GPU-abc123...",device="nvidia0",modelName="NVIDIA A100-PCIE-40GB"} 42
----

If you see metric output, the exporter is functioning correctly and ready for Prometheus collection.

=== Layer 3: Metric Collection and Storage

**OpenShift Monitoring Stack** provides the collection and storage tier:

**Prometheus**:
* Scrapes DCGM Exporter endpoints automatically
* Stores metrics with configurable retention (default: 15 days)
* Evaluates alerting rules for GPU health
* Provides PromQL query interface

**Thanos Querier**:
* Federates metrics from multiple Prometheus instances
* Provides long-term storage integration
* Enables cross-cluster metric queries
* Deduplicates metrics from HA Prometheus deployments

**User Workload Monitoring**:
* Separate Prometheus instance for application-level metrics
* Enables namespace-scoped metric collection
* Isolates tenant metrics from platform metrics
* Configurable resource limits and retention policies

[WARNING]
====
**Metric collection overhead considerations:**

* **GPU impact**: 30-second scrape intervals consume ~0.5% GPU utilization + 2-4MB memory per GPU
* **Network impact**: Each GPU generates ~15KB/scrape (450KB/minute, ~650MB/day per GPU)
* **Storage impact**: 100 GPUs with 15-day retention requires ~15GB Prometheus storage

**For clusters with >500 GPUs:**

* Increase scrape interval to 60 seconds (reduces overhead by 50%)
* Use custom DCGM metric profiles to collect only critical metrics
* Consider Prometheus remote write for long-term storage offloading
* Implement metric aggregation for fleet-wide dashboards

The overhead is negligible for typical deployments (<200 GPUs) but warrants optimization at scale.
====

=== Layer 4: Visualization and Analysis

**Grafana Operator** provides the visualization and dashboard layer through a standalone Grafana instance (deployed separately from OpenShift's built-in console Observe dashboards):

* Connects to Thanos Querier as primary data source
* Supports complex PromQL queries for metric correlation
* Provides pre-built NVIDIA DCGM dashboards via GrafanaDashboard CRs
* Enables custom dashboard creation for specific use cases
* Supports alerting, annotations, and templating
* Implements Dashboard-as-Code via GrafanaDashboard Custom Resources

**Why deploy Grafana Operator instead of using OpenShift console Observe dashboards?**

While the OpenShift web console provides basic metric visualization, the Grafana Operator enables production-grade GPU monitoring capabilities:

* **Custom dashboard development**: Build dashboards with PromQL variables, advanced panels (heatmaps, annotations), and MIG-aware filtering
* **Metric correlation**: Combine GPU metrics (DCGM), node metrics (CPU, memory), and inference workload metrics (vLLM latency, throughput) in unified dashboards
* **MIG-aware visualizations**: Filter and aggregate metrics by `GPU_I_ID` labels for per-instance tracking across multi-tenant deployments
* **Dashboard-as-Code**: Store dashboards as GrafanaDashboard CRs in Git for version control and GitOps workflows
* **Richer visualization**: Access to 200+ panel types, alert overlays, time-series annotations, and comparison views

The OpenShift console Observe section is excellent for quick metric inspection and troubleshooting, but Grafana provides the depth required for production GPU platform operations.

**Lab preview:** In xref:s3-grafana-setup-lab.adoc[Section 3], you'll deploy the Grafana Operator from OperatorHub, create a Grafana instance CR, and configure a GrafanaDatasource pointing to Thanos Querier. In xref:s4-dashboards-lab.adoc[Section 4], you'll import the official NVIDIA DCGM dashboard and build custom MIG-aware visualizations.

== Integration Architecture

The complete observability architecture connects multiple OpenShift subsystems:

```
┌─────────────────────────────────────────────────────────────┐
│ GPU Node (worker-gpu-0)                                     │
│  ┌──────────┐         ┌─────────────┐                       │
│  │  DCGM    │────────▶│ DCGM        │:9400                  │
│  │  Daemon  │         │ Exporter    │────┐                  │
│  └──────────┘         └─────────────┘    │                  │
│       │                                   │                  │
│    GPU Hardware                           │                  │
└───────────────────────────────────────────┼──────────────────┘
                                            │
                    ┌───────────────────────▼──────────────────┐
                    │ OpenShift Monitoring                     │
                    │  ┌────────────────┐   ┌───────────────┐ │
                    │  │ Prometheus     │──▶│ Thanos        │ │
                    │  │ (User Workload)│   │ Querier       │ │
                    │  └────────────────┘   └───────┬───────┘ │
                    └─────────────────────────────────┼─────────┘
                                                      │
                    ┌─────────────────────────────────▼─────────┐
                    │ Grafana Instance                          │
                    │  ┌──────────────────────────────────────┐ │
                    │  │ NVIDIA DCGM Dashboard                │ │
                    │  │  • GPU Utilization                   │ │
                    │  │  • Memory Usage                      │ │
                    │  │  • Temperature & Power               │ │
                    │  │  • MIG Instance Metrics              │ │
                    │  └──────────────────────────────────────┘ │
                    └───────────────────────────────────────────┘
```

== Key Observability Capabilities

=== Real-Time Monitoring

* Live GPU utilization and memory consumption
* Immediate visibility into hardware health issues
* Per-MIG-instance resource tracking
* Workload-to-GPU correlation

=== Historical Analysis

* Trend analysis for capacity planning
* Performance regression detection
* Cost optimization opportunities
* SLA compliance verification

=== Alerting and Anomaly Detection

* GPU temperature threshold alerts
* Memory exhaustion warnings
* Error rate spike detection
* Utilization anomaly detection

=== Multi-Dimensional Correlation

Correlate GPU metrics with:

* **Pod metrics**: Container CPU/memory, restart counts
* **Node metrics**: System CPU, network, disk I/O
* **Application metrics**: Inference latency, throughput, error rates
* **Platform metrics**: Scheduler decisions, resource quotas

This holistic view enables operators to:

* Identify GPU bottlenecks in the inference pipeline
* Correlate application performance degradation with hardware issues
* Optimize resource allocation based on actual usage patterns
* Troubleshoot complex issues spanning multiple infrastructure layers

== Understanding the GPU Metrics Landscape

GPU observability involves three distinct metric categories, each revealing different aspects of platform health and performance. Understanding this taxonomy helps platform engineers ask the right questions and build effective dashboards.

=== Metric Categories

[cols="2,3,3",options="header"]
|===
|Category |What It Reveals |Example Metrics

|**Infrastructure Metrics**
|Kubernetes resource allocation, scheduling decisions, pod lifecycle
|`kube_pod_container_resource_requests{resource="nvidia.com/gpu"}` +
`kube_node_status_allocatable{resource="nvidia.com/gpu"}` +
`kube_pod_status_phase{pod=~".*-gpu-.*"}`

|**Hardware Metrics**
|GPU device health, thermal performance, power consumption, error rates
|`DCGM_FI_DEV_GPU_UTIL` (utilization %) +
`DCGM_FI_DEV_FB_USED` (memory bytes) +
`DCGM_FI_DEV_GPU_TEMP` (temperature °C) +
`DCGM_FI_DEV_POWER_USAGE` (power watts) +
`DCGM_FI_DEV_XID_ERRORS` (critical errors)

|**Workload Metrics**
|Application performance, inference throughput, model-specific behavior
|`vllm:request_success_total` (inference requests) +
`vllm:time_to_first_token_seconds` (latency) +
`vllm:request_params_n` (batch size) +
`ovms_requests_success` (OpenVINO requests)
|===

=== Correlation Patterns for Troubleshooting

Combining metrics across categories reveals root causes that single-category analysis misses:

**Pattern 1: Memory Bottleneck**

* **Symptoms**: High `DCGM_FI_DEV_GPU_UTIL` (85%+) but low `vllm:request_success_total` (throughput drops 60%)
* **Root cause**: `DCGM_FI_DEV_FB_USED` at 38GB/40GB (95% memory) causing thrashing
* **Resolution**: Reduce batch size or migrate to MIG instance with more memory

**Pattern 2: OOM Kill Loop**

* **Symptoms**: Normal `DCGM_FI_DEV_GPU_UTIL` (45%) but `kube_pod_container_status_restarts_total` incrementing every 10 minutes
* **Root cause**: Pod memory limit too low for GPU memory allocation (host RAM constraint, not GPU VRAM)
* **Resolution**: Increase pod `resources.limits.memory` to accommodate GPU memory management overhead

**Pattern 3: Thermal Throttling**

* **Symptoms**: `DCGM_FI_DEV_GPU_TEMP` spikes to 87°C, followed by `DCGM_FI_DEV_SM_CLOCK` drop from 1410MHz to 1200MHz, causing `vllm:time_to_first_token_seconds` increase from 45ms to 120ms
* **Root cause**: Inadequate data center cooling or blocked airflow
* **Resolution**: Verify HVAC settings, check for physical obstructions, reduce GPU power limit temporarily

**Pattern 4: MIG Instance Imbalance**

* **Symptoms**: `DCGM_FI_DEV_GPU_UTIL{GPU_I_ID="0"}` at 95%, `DCGM_FI_DEV_GPU_UTIL{GPU_I_ID="1"}` at 12% on same physical GPU
* **Root cause**: Workload scheduling imbalance across MIG instances
* **Resolution**: Adjust pod resource requests or implement topology-aware scheduling

These patterns demonstrate why comprehensive observability across all three metric categories is essential for production GPU operations.

== Choosing Your Observability Strategy

Production GPU monitoring involves two strategic decisions: how to collect metrics and how to visualize them. These decisions depend on cluster size, organizational maturity, and integration requirements.

=== Decision Matrix 1: Metric Collection Strategy

[cols="2,2,2,2",options="header"]
|===
|Factor |OpenShift User-Workload Monitoring |Standalone Prometheus |Third-Party Observability

|**Best for**
|<200 GPUs, integrated with OpenShift
|Multi-cluster federation, >200 GPUs
|Enterprise-wide observability consolidation

|**GPU Support**
|✅ ServiceMonitor auto-discovery via GPU Operator
|✅ Manual scrape configuration required
|⚠️ Agent installation, vendor-specific integration

|**Setup Complexity**
|Low (enable ConfigMap + label namespace)
|Medium (deploy Prometheus + configure scrape targets)
|High (agent deployment, authentication, network policy)

|**Retention**
|15-30 days (configurable via PVC)
|Unlimited (constrained by storage only)
|Vendor-dependent (often 90+ days with tiered storage)

|**Query Interface**
|OpenShift console Observe + PromQL
|Prometheus UI + Grafana
|Vendor UI + API

|**Cost**
|Included with OpenShift subscription
|Infrastructure costs only (storage, compute)
|$$$/GPU/month or $$$/metric/month

|**High Availability**
|Built-in (Prometheus HA + Thanos)
|Manual configuration required
|Vendor-managed SLA

|**Multi-Cluster**
|Requires Thanos federation
|Native support with remote read/write
|Often built-in
|===

**Recommendation:** Start with OpenShift user-workload monitoring for deployments under 200 GPUs. It provides enterprise-grade reliability with zero licensing cost and native integration with the GPU Operator's ServiceMonitor pattern. For clusters exceeding 200 GPUs or requiring >90 day retention, add Prometheus remote write to push metrics to long-term storage (Thanos, Cortex, or vendor platforms).

=== Decision Matrix 2: Visualization Strategy

[cols="2,2,2,2",options="header"]
|===
|Factor |OpenShift Console Observe |Grafana Operator |Hosted Grafana Cloud

|**Best for**
|Quick metric inspection, troubleshooting
|Custom dashboard development, production monitoring
|Enterprise-wide metric consolidation

|**Dashboard Customization**
|Limited (predefined queries, basic graphs)
|Full control (PromQL variables, 200+ panel types, templating)
|Full control + SaaS management

|**MIG-Aware Visualizations**
|Manual PromQL queries only
|GrafanaDashboard CRs with `GPU_I_ID` filters, per-instance panels
|Pre-built templates (vendor-dependent)

|**Metric Correlation**
|Single metric view (CPU, memory, GPU separate)
|Unified dashboards (GPU + node + workload metrics)
|Cross-cluster correlation

|**Integration Effort**
|Zero (built-in to OpenShift)
|Low (deploy operator + create Grafana CR + configure datasource)
|Medium (API key + remote write configuration)

|**Dashboard-as-Code**
|❌ Not supported
|✅ GrafanaDashboard CRs (Git + GitOps)
|✅ API-driven provisioning

|**Alert Management**
|OpenShift Alertmanager (separate from dashboards)
|Grafana alerting (unified with dashboards)
|Grafana Cloud alerting

|**Cost**
|Included with OpenShift
|Included (operator overhead: ~200MB RAM)
|$$$/user/month or $$$/metric/month
|===

**Recommendation:** Deploy the Grafana Operator for production GPU monitoring (covered in xref:s3-grafana-setup-lab.adoc[Section 3 lab]). While the OpenShift console Observe section is excellent for quick troubleshooting ("Why is this pod restarting?"), Grafana enables the correlation dashboards required for GPU platform operations: combining DCGM hardware metrics with vLLM inference performance, filtering by MIG instance, and tracking trends across days or weeks. The ability to store dashboards as GrafanaDashboard CRs also enables infrastructure-as-code workflows and dashboard versioning.

[TIP]
====
**Hybrid approach for best results:**

1. **Use OpenShift console Observe for**: Quick metric queries during incident response, exploring available metrics, validating Prometheus is scraping correctly
2. **Use Grafana for**: Production dashboards, capacity planning analysis, SLA tracking, MIG utilization heatmaps, multi-metric correlation panels

This combination provides both rapid troubleshooting access and comprehensive production monitoring without sacrificing either capability.
====

== What's Next: Hands-On Observability Implementation

You now understand the four-layer observability architecture, the business impact of GPU monitoring, and the strategic decisions for metric collection and visualization. In the next three labs, you'll build a complete production-ready GPU observability stack.

=== Section 2 Lab: Expose GPU Metrics

Enable OpenShift user-workload monitoring and verify Prometheus is scraping DCGM Exporter metrics.

**You'll execute:**

* Modify `cluster-monitoring-config` ConfigMap to enable user-workload monitoring
* Label the `nvidia-gpu-operator` namespace for Prometheus discovery: `openshift.io/cluster-monitoring=true`
* Verify the ServiceMonitor is discovered: `oc get servicemonitor -n nvidia-gpu-operator`
* Query live GPU metrics with PromQL in the OpenShift console:
** `DCGM_FI_DEV_GPU_UTIL` (real-time GPU utilization across all nodes)
** `DCGM_FI_DEV_FB_USED{GPU_I_ID=~".*"}` (per-MIG-instance memory usage)

**Outcome:** Production metrics pipeline collecting GPU telemetry every 30 seconds with 15-day retention.

=== Section 3 Lab: Deploy Grafana Operator

Install the Grafana Operator from OperatorHub, create a Grafana instance Custom Resource, and configure secure metric access.

**You'll execute:**

* Deploy Grafana Operator via Subscription CR (channel: `v5`)
* Create Grafana instance CR with OpenShift route and admin credentials
* Create ServiceAccount with `cluster-monitoring-view` ClusterRoleBinding for secure Thanos Querier access
* Configure GrafanaDatasource CR pointing to `https://thanos-querier.openshift-monitoring.svc:9091`
* Verify Grafana can query GPU metrics

**Outcome:** Standalone Grafana service accessible via OpenShift route with authenticated access to cluster metrics.

=== Section 4 Lab: Build Custom Dashboards

Import the official NVIDIA DCGM Exporter dashboard and build custom visualizations for MIG-aware monitoring.

**You'll execute:**

* Download NVIDIA DCGM dashboard JSON from GitHub
* Create GrafanaDashboard CR to import the official dashboard
* Explore GPU utilization, memory, temperature, and power panels
* Build custom dashboard combining:
** DCGM hardware metrics (`DCGM_FI_DEV_GPU_UTIL`, `DCGM_FI_DEV_GPU_TEMP`)
** Node infrastructure metrics (`node_cpu_seconds_total`, `node_memory_MemAvailable_bytes`)
** MIG instance allocation (grouped by `GPU_I_ID` label)
* Add dashboard variables for filtering by node, GPU, and MIG instance

**Outcome:** Production-ready dashboards providing unified view of GPU hardware health, node infrastructure, and MIG instance utilization for multi-tenant monitoring.

**End state after completing all three labs:** A complete GPU observability stack with automated metric collection, 15-day retention, custom Grafana dashboards, MIG-aware visualizations, and production alert thresholds ready for your Models-as-a-Service platform.

////
**The MaaS Observability Stack:**
Understand how hardware telemetry flows up to the platform observability plane. Discuss the integration between OpenShift's built-in monitoring (Prometheus/Thanos) and standalone visualization using the Grafana Operator for platform, node, and accelerator metric correlation.
////
