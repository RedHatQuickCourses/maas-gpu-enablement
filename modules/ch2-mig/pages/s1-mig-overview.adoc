:time_estimate: 20

= Maximizing GPU ROI with Multi-Instance GPU

_Estimated reading time: *{time_estimate} minutes*._

Objective::

Understand how the Multi-Instance GPU (MIG) feature splits hardware resources into multiple GPU instances that operate with complete isolation from each other. Evaluate MIG advertisement strategies including Single (homogeneous) and Mixed (heterogeneous) slicing approaches.

== The GPU Utilization Challenge

High-performance GPUs like the NVIDIA A100 and H100 represent significant capital investment, often costing $10,000-$30,000 per unit. However, many AI inference workloads don't require the full computational capacity of these GPUs, leading to resource underutilization and poor return on investment.

=== The Multi-Tenant Platform Scenario

Consider a production AI platform serving three teams with a single NVIDIA A100-40GB GPU ($15,000 investment):

* **Team A (ML Platform)**: LLaMA-7B inference service requiring 14GB VRAM and 50% compute capacity
* **Team B (Data Science)**: BERT embedding generation requiring 4GB VRAM and 10% compute capacity
* **Team C (Computer Vision)**: ResNet-50 image classification requiring 3GB VRAM and 15% compute capacity

**Total actual requirements**: 21GB VRAM (53% of available 40GB) and 75% compute capacity

**Traditional approach (full GPU allocation)**: Only one team can use the GPU at any given time, forcing the other teams to wait in queue or requiring purchase of additional GPUs.

=== Before and After MIG: Resource Utilization

[cols="2,2,2",options="header"]
|===
|Metric |Without MIG (Full GPU) |With MIG (3g.20gb + 2g.10gb + 2g.10gb)

|**GPU Utilization**
|33% average (only Team A runs)
|78% average (all 3 teams concurrent)

|**Workloads Supported**
|1 at a time (serialized)
|3 concurrent (isolated)

|**Idle Time**
|60% (waiting for availability)
|15% (near-continuous utilization)

|**Cost per Workload**
|$15,000 / 1 = $15,000
|$15,000 / 3 = $5,000

|**Performance Isolation**
|N/A (exclusive access)
|✅ Hardware-guaranteed (<1% P99 variance)

|**ROI Achievement**
|24 months baseline
|10 months (2.4x faster)
|===

This represents a **7x improvement in workload density** and **$10,000 cost savings per additional workload** compared to purchasing separate GPUs.

=== Traditional GPU Sharing Approaches

Before MIG, platform engineers faced limited options:

* **Time-slicing**: Multiple workloads share a GPU through temporal scheduling, but suffer 15-40% P99 latency variance due to resource contention
* **GPU passthrough**: Each workload gets exclusive access to a full GPU, maximizing performance but achieving only 20-40% utilization for inference workloads
* **Virtual GPUs (vGPU)**: Requires additional NVIDIA licensing and virtualization overhead, adding operational complexity

Multi-Instance GPU (MIG) provides a hardware-level solution that balances performance isolation with resource efficiency, achieving the utilization benefits of time-slicing while maintaining the predictable performance of full GPU allocation.

== What is Multi-Instance GPU?

Multi-Instance GPU is a hardware capability available on NVIDIA Ampere and Hopper architecture GPUs (A30, A100, H100, etc.) that partitions a single physical GPU into multiple isolated GPU instances. Each instance has:

* **Dedicated GPU compute resources** (streaming multiprocessors)
* **Dedicated memory** (a slice of the total GPU VRAM)
* **Dedicated memory bandwidth** (isolated memory controllers)
* **Complete fault isolation** (errors in one instance don't affect others)

From the perspective of containerized workloads, each MIG instance appears as a separate GPU device with predictable, guaranteed performance.

=== How MIG Partitions Physical Hardware

Understanding MIG at the hardware level clarifies why it provides true performance isolation unlike software-based approaches.

**A100-40GB Architecture**:

* **108 Streaming Multiprocessors (SMs)**: The compute units that execute CUDA kernels
* **40GB HBM2 memory**: High-bandwidth GPU framebuffer
* **8 memory controllers**: Provide memory bandwidth to SMs

When MIG mode is enabled, NVIDIA reserves one compute unit for resource management. The remaining 7/8ths of resources are available for partitioning.

**Example: 3g.20gb Profile**:

* Allocates 3/7ths of usable SMs (approximately 46 streaming multiprocessors)
* Allocates 20GB dedicated VRAM partition
* Allocates dedicated memory controllers for isolated bandwidth
* Creates hardware-enforced barriers preventing cross-instance interference

[NOTE]
====
**MIG vs. Time-Slicing: A Critical Difference**

* **Time-slicing**: Software multiplexing where workloads share the same physical resources temporally. Workload A uses the GPU for 100ms, then Workload B, then Workload A again. All 108 SMs and 40GB are shared.

* **MIG**: Physical partitioning where resources are dedicated. Workload A always uses its 46 SMs and 20GB. Workload B uses different physical SMs and memory regions. No temporal sharing—workloads run truly concurrently.

This architectural difference is why MIG achieves <1% P99 latency variance while time-slicing experiences 15-40% variance.
====

=== MIG Partition Visualization

----
Physical A100-40GB GPU Partitioning Example:

┌─────────────────────────────────────────────────┐
│  Physical GPU: A100-40GB (108 SMs, 40GB VRAM)   │
│  MIG Mode: Enabled (7/8 available for slicing)  │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────────────────┐  MIG Instance 0  │
│  │  3g.20gb Profile         │  ~46 SMs         │
│  │  Team A: LLaMA-7B        │  20GB VRAM       │
│  │                          │  Dedicated       │
│  └──────────────────────────┘  Memory BW       │
│                                                 │
│  ┌─────────────┐              MIG Instance 1  │
│  │ 2g.10gb     │              ~31 SMs         │
│  │ Team B:     │              10GB VRAM       │
│  │ BERT        │              Dedicated       │
│  └─────────────┘              Memory BW       │
│                                                 │
│  ┌─────────────┐              MIG Instance 2  │
│  │ 2g.10gb     │              ~31 SMs         │
│  │ Team C:     │              10GB VRAM       │
│  │ ResNet-50   │              Dedicated       │
│  └─────────────┘              Memory BW       │
│                                                 │
│  Reserved: ~4 SMs for MIG management            │
└─────────────────────────────────────────────────┘
----

=== Kubernetes Integration

From Kubernetes' perspective, MIG instances appear as distinct allocatable resources. The GPU Operator components from Chapter 1 coordinate to discover and advertise these resources:

1. **MIG Manager**: Applies MIG configuration based on node labels (e.g., `nvidia.com/mig.config=all-2g.10gb`)
2. **GPU Feature Discovery (GFD)**: Scans GPU capabilities using `nvidia-smi`, detects MIG instances, applies node labels like `nvidia.com/mig-2g.10gb.count=3`
3. **Device Plugin**: Reads GFD labels and advertises MIG instances as allocatable resources to the Kubernetes scheduler
4. **Scheduler**: Places pods requesting `nvidia.com/mig-2g.10gb: 1` on nodes with available instances

This integration means MIG instances work with standard Kubernetes resource requests—no custom schedulers or admission controllers required for basic functionality.

== MIG Profile Architecture and Planning

A physical GPU can be partitioned into MIG instances with different profiles. Understanding profile characteristics and planning your partitioning strategy is essential for maximizing utilization while meeting workload requirements.

=== MIG Profile Characteristics

The profile naming convention `Xg.YYgb` encodes key resource allocations:

* **X**: Compute slice size (in 1/7ths of total usable GPU compute)
* **YY**: Memory allocation in gigabytes

For example, `3g.20gb` means: 3/7ths of compute resources (approximately 46 streaming multiprocessors on A100-40GB) and 20GB dedicated VRAM.

=== Common MIG Profiles by GPU Model

**A100-40GB Profiles**:

[cols="2,1,2,1,3"]
|===
|Profile |Max Instances |Memory per Instance |Compute Slices |Typical Use Case

|`1g.5gb`
|7
|5 GB
|1/7 (~15 SMs)
|Small models: DistilBERT, small CNNs, development

|`2g.10gb`
|3
|10 GB
|2/7 (~31 SMs)
|Medium models: BERT-base, ResNet-50, fine-tuning

|`3g.20gb`
|2
|20 GB
|3/7 (~46 SMs)
|Large models: LLaMA-7B, GPT-J-6B, training

|`4g.20gb`
|1
|20 GB
|4/7 (~62 SMs)
|Very large inference, more compute-intensive workloads

|`7g.40gb`
|1
|40 GB
|7/7 (full GPU)
|Full GPU mode (MIG disabled or single instance)
|===

**A100-80GB Profiles**: Profiles scale with double memory: `1g.10gb` (7 instances), `2g.20gb` (3 instances), `3g.40gb` (2 instances), `7g.80gb` (1 instance).

**A30-24GB Profiles**: Smaller GPU with up to 4 instances: `1g.6gb` (4 instances), `2g.12gb` (2 instances), `4g.24gb` (1 instance).

[IMPORTANT]
====
When MIG mode is enabled, NVIDIA reserves one compute unit (1/8th of the GPU) for MIG resource management. The remaining 7/8ths are available for partitioning into instances. This is why maximum instances sum to 7g, not 8g.
====

=== Selecting MIG Profiles for Your Workloads

[TIP]
====
**Profile Selection Decision Process:**

1. **Inventory workload requirements**:
   - Measure actual VRAM usage during test runs (`nvidia-smi` inside pods)
   - Profile compute utilization under realistic load
   - Count how many concurrent instances you need

2. **Add overhead buffer**:
   - Add 20-25% to VRAM requirement for framework overhead (PyTorch, TensorFlow allocators)
   - Example: Model uses 3.8GB → Plan for 4.8GB → Choose `1g.5gb` profile

3. **Map to nearest profile**:
   - Always round UP to the next available profile
   - Example: 12GB requirement → Choose `3g.20gb` (not `2g.10gb`)

4. **Validate profile combination**:
   - Ensure instance counts fit: 7x `1g.5gb`, or 3x `2g.10gb`, or 1x `3g.20gb` + 2x `2g.10gb`
   - Consult NVIDIA MIG User Guide for heterogeneous combinations
====

=== Common MIG Profile Combinations

Beyond homogeneous configurations (all instances same profile), you can mix profiles for diverse workloads:

[cols="3,2,2,3"]
|===
|Use Case |Profile Combination |Total Instances |Utilization Strategy

|**Uniform small models**
|7x `1g.5gb`
|7
|Maximum density for microservices, dev/test

|**Uniform medium models**
|3x `2g.10gb`
|3
|Balanced for BERT-class workloads

|**Mixed workload tiers**
|1x `3g.20gb` + 2x `2g.10gb`
|3
|One large model + two medium models

|**Balanced heterogeneous**
|2x `1g.5gb` + 1x `2g.10gb` + 1x `3g.20gb`
|4
|Small, medium, large workload mix (all-balanced profile)

|**Large + development**
|1x `4g.20gb` + 2x `1g.5gb` + 1x `1g.10gb`†
|4
|Production large model + testing instances
|===

† This combination requires careful validation against the MIG User Guide for placement constraints.

[NOTE]
====
The `all-balanced` built-in profile automatically configures a heterogeneous mix optimized for the GPU model. For A100-40GB, this creates 2x `1g.5gb` + 1x `2g.10gb` + 1x `3g.20gb`, providing four instances spanning small, medium, and large workload sizes.
====

== MIG Advertisement Strategies

The NVIDIA GPU Operator supports two strategies for exposing MIG instances to the Kubernetes scheduler. This strategy choice fundamentally affects how workloads request resources and how you manage node configurations.

=== Single Strategy (Homogeneous)

All MIG instances on a node must use the same profile. Instances are advertised as `nvidia.com/gpu` resources, providing **backward compatibility** with existing workloads.

**Characteristics**:

* **Resource advertisement**: `nvidia.com/gpu: 7` (for 7x `1g.5gb` instances)
* **Node configuration**: All GPUs on node must use identical profile
* **Workload requests**: Standard `nvidia.com/gpu: 1` (no profile-specific request)
* **Scheduling**: Kubernetes scheduler treats MIG instances identically to full GPUs

**Example**: A100-40GB cluster with `all-3g.20gb` profile:

* Each physical GPU creates 2 MIG instances
* Node with 4 A100 GPUs advertises `nvidia.com/gpu: 8`
* Existing inference services request `nvidia.com/gpu: 1` without modification

**Advantages**:

* Easy to understand and manage
* Backward compatible with existing pod specifications
* Predictable resource allocation (all instances identical)
* Simple capacity planning (uniform node pools)
* Best for uniform workload patterns

=== Mixed Strategy (Heterogeneous)

Different MIG profiles can coexist on the same physical GPU. Instances are advertised with **profile-specific resource names**.

**Characteristics**:

* **Resource advertisement**: `nvidia.com/mig-1g.5gb: 2`, `nvidia.com/mig-2g.10gb: 1`, `nvidia.com/mig-3g.20gb: 1`
* **Node configuration**: Each GPU can have different profile mix
* **Workload requests**: Must specify profile: `nvidia.com/mig-3g.20gb: 1`
* **Scheduling**: Kubernetes scheduler matches request to available profile

**Example**: A100-40GB node with `all-balanced` profile:

* Creates 2x `1g.5gb`, 1x `2g.10gb`, 1x `3g.20gb` per GPU
* Small models request `nvidia.com/mig-1g.5gb: 1`
* Large models request `nvidia.com/mig-3g.20gb: 1`
* Scheduler places workloads on nodes with matching profiles

**Advantages**:

* Accommodates diverse workload requirements on single node
* Optimizes resource utilization for heterogeneous platforms
* Maximum flexibility for workload placement
* Supports true multi-tenant deployments with varied resource needs

**Challenges**:

* More complex to manage and schedule
* Workload specifications must be profile-aware
* Per-GPU configuration tracking required
* Risk of scheduling failures if pod requests unavailable profile

=== Production Strategy Selection Matrix

[cols="2,2,2,2"]
|===
|Criterion |Single Strategy |Mixed Strategy |Production Guidance

|**Operational Complexity**
|✅ Low - uniform config
|❌ High - per-GPU tracking
|Single for teams <10 engineers

|**Resource Efficiency**
|⚠️ May waste resources if workloads vary
|✅ Optimal packing for diverse workloads
|Mixed for heterogeneous platforms

|**Scheduling Simplicity**
|✅ Simple - all instances identical
|⚠️ Complex - must match profiles
|Single for <50 total workloads

|**Backward Compatibility**
|✅ Existing pods work unchanged
|❌ Pods must specify MIG profiles
|Single for migrating existing apps

|**Reconfiguration Impact**
|✅ Change all nodes uniformly
|❌ Per-node config drift risk
|Single for stability-focused platforms

|**Multi-Tenancy Support**
|⚠️ Limited - all tenants same size
|✅ Excellent - right-size per tenant
|Mixed for true SaaS platforms

|**Cluster Size Sweet Spot**
|✅ Large clusters (node pools)
|✅ Small clusters (diverse nodes)
|Depends on node count and diversity
|===

[WARNING]
====
**Mixed Strategy Requires Profile-Aware Scheduling**

With mixed strategy, pods **must** request specific MIG profiles in resource limits:

```yaml
resources:
  limits:
    nvidia.com/mig-3g.20gb: 1  # Correct for mixed
```

Requesting generic `nvidia.com/gpu: 1` will **fail to schedule** on MIG-enabled nodes using mixed strategy. You must either:

* Update all pod specifications to request specific profiles
* Implement admission webhooks to inject correct resource requests
* Use Kueue ResourceFlavors (covered in later chapters) for automated profile selection
====

[TIP]
====
**Recommended Adoption Path: Start Single, Migrate to Mixed**

For teams new to MIG:

1. **Months 1-3**: Deploy Single strategy with one profile (e.g., `all-2g.10gb`)
   - Learn MIG operational patterns
   - Measure actual workload resource usage
   - Build confidence with uniform configuration

2. **Months 4-6**: Evaluate resource waste and workload diversity
   - Analyze DCGM metrics for over/under-provisioning
   - Identify distinct workload tiers (small, medium, large)
   - Calculate potential efficiency gains from mixed profiles

3. **Months 7+**: Migrate to Mixed strategy if justified
   - Migrate when >20% resource waste from uniform sizing
   - Requires dedicated platform team for GPU operations
   - Implement profile-aware admission controls first

This phased approach balances operational learning with efficiency optimization.
====

== How MIG Partitioning Works in Kubernetes

MIG partitioning in OpenShift involves coordination between the GPU Operator components you deployed in Chapter 1. Understanding this workflow helps you troubleshoot configuration issues and plan maintenance windows effectively.

=== Component Roles in MIG Lifecycle

From Chapter 1, recall these GPU Operator components and their specific roles in MIG enablement:

* **MIG Manager**: Watches node labels, validates MIG profiles, applies MIG configuration via `nvidia-smi mig` commands
* **GPU Feature Discovery (GFD)**: Scans GPUs using NVML (NVIDIA Management Library), detects MIG instances, applies detailed node labels
* **Device Plugin**: Reads GFD-generated labels, advertises MIG instances as allocatable Kubernetes resources
* **NVIDIA Driver**: Provides kernel-level MIG mode enablement, instance creation, and hardware partitioning

These components coordinate through the operator reconciliation pattern you learned in Chapter 1, Section 2.

=== MIG Enablement Workflow

When you label a node with `nvidia.com/mig.config=all-2g.10gb`, this workflow executes:

----
1. MIG Manager DaemonSet (controller) detects label change
         ↓
2. MIG Manager validates profile against GPU capability
   - Checks GPU model supports requested profile
   - Verifies profile name exists in ConfigMap
         ↓
3. MIG Manager enables MIG mode on GPU (if not already enabled)
   - Executes: nvidia-smi -i 0 -mig 1
   - Requires GPU reset (brief workload interruption)
   - Takes ~10 seconds per GPU
         ↓
4. MIG Manager creates MIG instances per profile
   - Partitions SMs and memory per profile spec
   - Creates device handles for each instance
   - Takes ~30 seconds for profile application
         ↓
5. GPU Feature Discovery rescans GPU capabilities
   - Queries MIG instances via nvidia-smi
   - Detects MIG geometry and resource allocations
   - Applies labels: nvidia.com/mig-2g.10gb.count=3
         ↓
6. Device Plugin rediscovers allocatable resources
   - Reads GFD-generated MIG labels
   - Advertises: nvidia.com/mig-2g.10gb: 3
   - Updates Node.status.allocatable
         ↓
7. Kubernetes Scheduler sees new resources available
   - Pods requesting nvidia.com/mig-2g.10gb can schedule
   - Workloads begin utilizing MIG instances
----

**Total Time**: 10-20 minutes from label application to resource availability (OpenShift documentation guidance)

[WARNING]
====
**MIG Reconfiguration Requires Workload Drain**

Changing MIG profiles on a node with running GPU workloads requires these steps:

1. **Cordon node**: `oc adm cordon worker-gpu-0` (prevent new pod scheduling)
2. **Drain node**: `oc adm drain worker-gpu-0 --ignore-daemonsets --delete-emptydir-data` (evict running pods, 5-10 min for workload migration)
3. **Apply new label**: `oc label node worker-gpu-0 nvidia.com/mig.config=all-3g.20gb --overwrite`
4. **Wait for reconfiguration**: Monitor MIG Manager logs (10-20 min)
5. **Uncordon node**: `oc adm uncordon worker-gpu-0` (allow new workloads)

**Plan MIG configuration changes during maintenance windows.** Unlike time-slicing (which only restarts device plugin pods), MIG mode changes reset the GPU hardware, terminating all running workloads on that GPU.
====

=== Integration with ClusterPolicy (Chapter 1)

The ClusterPolicy Custom Resource you configured in Chapter 1 enables MIG Manager capability:

[source,yaml]
----
apiVersion: nvidia.com/v1
kind: ClusterPolicy
metadata:
  name: gpu-cluster-policy
spec:
  migManager:
    enabled: true  # <1>
  mig:
    strategy: mixed  # <2>
----
<1> Deploys MIG Manager DaemonSet—actual profile configuration happens via node labels
<2> Sets advertisement strategy (single or mixed) for cluster-wide MIG resource naming

This **decouples MIG capability from MIG configuration**:

* **Capability** (ClusterPolicy): Enables MIG Manager cluster-wide, sets strategy
* **Configuration** (node labels): Applies specific profiles per node

This design allows you to:

* Enable MIG capability cluster-wide once via ClusterPolicy
* Apply MIG profiles per-node dynamically via labels
* Change profiles without modifying ClusterPolicy or restarting operators

[TIP]
====
**Enable MIG Manager Even If Not Immediately Using MIG**

Set `migManager.enabled: true` in ClusterPolicy during initial GPU Operator deployment, even if you don't plan to use MIG immediately. This deploys the MIG Manager DaemonSet in standby mode, allowing you to enable MIG on individual nodes later via labels without cluster-wide operator reconfiguration.

This follows the production best practice from Chapter 1, Section 3: prepare infrastructure capabilities early, activate them as needed.
====

=== MIG Configuration Auto-Generation

Starting with GPU Operator v26.3.0, MIG Manager **automatically generates** MIG configurations for your hardware:

* Queries GPU capabilities via NVML (NVIDIA Management Library)
* Discovers supported MIG profiles for each GPU model
* Creates a ConfigMap named `<node-name>-mig-config` for each MIG-capable node
* Includes built-in profiles: `all-disabled`, `all-balanced`, and per-profile configs (e.g., `all-1g.10gb`, `all-2g.10gb`)

**Legacy behavior (pre-v26.3.0)**: Used static ConfigMap `default-mig-parted-config` with predefined profiles.

This auto-generation means you typically don't need custom ConfigMaps unless you require specialized heterogeneous combinations not covered by `all-balanced`.

== Enabling MIG via Node Labels

MIG configuration uses a label-driven workflow. The MIG Manager watches for specific labels on GPU nodes and applies the corresponding profile from its configuration.

=== Label-Driven Configuration Pattern

The GPU Operator uses this label schema:

[source,bash]
----
nvidia.com/mig.config=<profile-name>
----

**Built-in Profile Names** (no additional ConfigMap configuration needed):

* `all-disabled` - MIG mode off, GPU operates as single full device (default)
* `all-1g.5gb` - Partition into 7x `1g.5gb` instances (A100-40GB)
* `all-1g.10gb` - Partition into 7x `1g.10gb` instances (A100-80GB)
* `all-2g.10gb` - Partition into 3x `2g.10gb` instances (A100-40GB)
* `all-3g.20gb` - Partition into 2x `3g.20gb` instances (A100-40GB)
* `all-balanced` - Balanced heterogeneous mix (2x `1g.5gb` + 1x `2g.10gb` + 1x `3g.20gb` on A100-40GB)

**Example: Applying MIG Profile to Nodes**

[source,bash]
----
# Set MIG strategy in ClusterPolicy (one-time cluster-wide configuration)
$ oc patch clusterpolicy/gpu-cluster-policy --type='json' \
  -p='[{"op": "replace", "path": "/spec/mig/strategy", "value": "mixed"}]'

# Label GPU nodes for MIG configuration
$ oc label node worker-gpu-0.example.com nvidia.com/mig.config=all-2g.10gb --overwrite
node/worker-gpu-0.example.com labeled

$ oc label node worker-gpu-1.example.com nvidia.com/mig.config=all-2g.10gb --overwrite
node/worker-gpu-1.example.com labeled

# Monitor MIG Manager applying configuration
$ oc logs -n nvidia-gpu-operator -l app=nvidia-mig-manager -f
time="2024-04-10T14:23:15Z" level=info msg="Applying MIG config" node=worker-gpu-0 profile=all-2g.10gb
time="2024-04-10T14:23:45Z" level=info msg="MIG configuration complete" instances=3

# Verify MIG instances are advertised (wait 10-20 minutes)
$ oc describe node worker-gpu-0.example.com | grep -A 10 "Allocatable:"
Allocatable:
  cpu:                          31500m
  memory:                       252455456Ki
  nvidia.com/mig-2g.10gb:       3  # <1>
  pods:                         250
----
<1> Three MIG instances now available for workload scheduling

=== Custom MIG Profiles via ConfigMap

For mixed profiles or custom combinations beyond `all-balanced`, create a `mig-parted` configuration:

[source,yaml]
----
apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-mig-parted-config
  namespace: nvidia-gpu-operator
data:
  config.yaml: |
    version: v1
    mig-configs:
      mixed-inference:  # <1>
        - devices: all
          mig-enabled: true
          mig-devices:
            "3g.20gb": 1  # <2>
            "2g.10gb": 2  # <3>
----
<1> Custom profile name for node labeling
<2> One large instance for LLaMA-7B inference
<3> Two medium instances for BERT-class models

**Apply Custom Profile to ClusterPolicy:**

[source,bash]
----
$ oc patch clusterpolicy gpu-cluster-policy --type=merge -p '
  {
    "spec": {
      "migManager": {
        "config": {
          "name": "custom-mig-parted-config"  # <1>
        }
      }
    }
  }'
----
<1> Reference your custom ConfigMap

**Use Custom Profile on Nodes:**

[source,bash]
----
$ oc label node worker-gpu-2.example.com \
  nvidia.com/mig.config=mixed-inference  # <1>
----
<1> Must match profile name from ConfigMap `mig-configs` section

[NOTE]
====
You'll apply these configurations hands-on in Section 2's lab. This overview prepares you for the practical workflow and troubleshooting during the lab.

For exhaustive lists of supported MIG profile combinations and placement constraints, consult the [NVIDIA MIG User Guide](https://docs.nvidia.com/datacenter/tesla/mig-user-guide/).
====

== Production Decision Matrix: Choosing Your GPU Sharing Strategy

Based on your workload characteristics and operational constraints, choose the appropriate GPU sharing strategy. This matrix synthesizes insights from Chapter 1's time-slicing coverage with MIG capabilities explored in this chapter.

=== Comprehensive Strategy Comparison

[cols="2,2,2,2"]
|===
|Criterion |Full GPU (Exclusive) |Time-Slicing (Software) |MIG (Hardware)

|**Performance Isolation**
|✅ Complete isolation
|❌ No isolation, contention risk
|✅ Hardware-guaranteed isolation

|**Workload Density**
|❌ 1 workload per GPU
|✅ 2-16x oversubscription
|✅ 2-7x true partitioning

|**Latency Predictability**
|✅ Fully predictable
|❌ 15-40% P99 variance
|✅ <1% P99 variance

|**GPU Utilization (Inference)**
|❌ 20-40% typical
|✅ 60-80% typical
|✅ 78-95% typical

|**Hardware Requirements**
|✅ Any NVIDIA GPU
|✅ Any NVIDIA GPU
|❌ A30/A100/H100 only

|**Reconfiguration Downtime**
|✅ None (just reschedule pods)
|⚠️ 2-3 min (device plugin restart)
|❌ 10-20 min (node drain + GPU reset)

|**Operational Complexity**
|✅ Simple (native Kubernetes)
|⚠️ Moderate (ConfigMap management)
|⚠️ Moderate (label-driven config)

|**Cost Efficiency (ROI)**
|❌ Poor (low utilization)
|✅ Good (2-3x improvement)
|✅ Excellent (4-7x improvement)

|**SLA Compliance Capability**
|✅ Easy to guarantee
|❌ Impossible (shared contention)
|✅ Easy to guarantee

|**Memory Fault Isolation**
|✅ Complete
|❌ OOM affects all workloads
|✅ Complete (hardware boundaries)
|===

=== Production Recommendation Decision Flow

----
Do you have SLA-backed latency requirements?
│
├─ YES → Do you have A30/A100/H100 GPUs?
│  │
│  ├─ YES → Use MIG
│  │        ✅ Hardware isolation + high density
│  │        ✅ Predictable performance + cost efficiency
│  │
│  └─ NO → Use Full GPU
│           ⚠️ Lower density but guaranteed performance
│           ⚠️ Consider upgrading to MIG-capable GPUs
│
└─ NO → Is this production (revenue-generating)?
   │
   ├─ YES → Do you have MIG-capable GPUs?
   │  │
   │  ├─ YES → Use MIG
   │  │        ✅ Better utilization than time-slicing
   │  │        ✅ Fault isolation protects workloads
   │  │
   │  └─ NO → Use Time-Slicing
   │           ✅ Better than full GPU for cost efficiency
   │           ⚠️ Monitor for performance variability
   │
   └─ NO → Use Time-Slicing
           ✅ Dev/test environments, maximize density
           ✅ Cost optimization priority
----

[IMPORTANT]
====
**Multi-Strategy Deployments in Large Platforms**

Production platforms often use **multiple strategies simultaneously** based on workload characteristics:

* **Training node pool**: Full GPU allocation
  - Workloads need maximum GPU memory and compute
  - Checkpointing requires exclusive access
  - Label: `node-role.kubernetes.io/gpu-training=true`

* **Inference node pool (A100-equipped)**: MIG partitioning
  - Multi-tenant model serving with SLA requirements
  - Hardware isolation for predictable latency
  - Label: `node-role.kubernetes.io/gpu-inference-mig=true`

* **Inference node pool (T4/V100-equipped)**: Time-slicing
  - No MIG support on these older architectures
  - Acceptable for non-SLA workloads
  - Label: `node-role.kubernetes.io/gpu-inference-timeslice=true`

Use node selectors and taints/tolerations to route workloads to appropriate node pools:

```yaml
nodeSelector:
  node-role.kubernetes.io/gpu-inference-mig: "true"
```

This architecture provides workload placement flexibility while optimizing each pool for its use case.
====

== MIG Benefits for MaaS

For Models-as-a-Service architectures, MIG provides measurable advantages that directly impact platform economics and operational reliability.

=== Cost Efficiency and ROI

* **Deploy 7 concurrent small models** on single A100-40GB (vs. 1 with full GPU allocation)
* **Reduce per-model GPU cost from $15,000 to $2,143** (7x cost reduction for `1g.5gb` profiles)
* **Increase cluster-wide GPU utilization from 33% to 78%** (2.4x improvement over exclusive allocation)
* **Achieve ROI break-even 2.4x faster** than full GPU deployments (10 months vs. 24 months baseline)
* **Right-size GPU resources** to actual model requirements, eliminating overprovisioning waste

**Concrete example**: Platform serving 21 small models previously required 21x A100 GPUs ($315,000 capital). With MIG `1g.5gb` profiles, same workload runs on 3x A100 GPUs ($45,000 capital), saving $270,000.

=== Performance Isolation and Predictability

* **Guarantee P99 latency variance <1%** (vs. 15-40% with time-slicing)
* **Hardware-enforced memory isolation** prevents out-of-memory (OOM) crosstalk between workloads
* **Dedicated streaming multiprocessors** eliminate compute contention and throttling
* **Enable SLA-backed inference** with measurable, enforceable performance guarantees
* **Prevent noisy neighbor problems** through physical resource partitioning

Time-slicing workloads experience latency spikes when concurrent requests arrive. MIG instances maintain consistent latency regardless of neighboring workload activity.

=== Operational Flexibility and Multi-Tenancy

* **Scale model instances independently** without affecting neighboring services (e.g., scale LLaMA-7B from 1 to 3 replicas without reprovisioning hardware)
* **Mix small and large models** on same physical GPU (e.g., `1g.5gb` microservices alongside `3g.20gb` large language models)
* **Support true multi-tenant deployments** with hardware isolation between teams
* **Assign dedicated MIG instances** to specific tenants or namespaces for guaranteed capacity
* **Enable chargeback and cost allocation** with accurate per-MIG-instance metrics from DCGM

=== Resource Predictability and Capacity Planning

* **Guaranteed GPU resources per deployment**: Each InferenceService gets dedicated SMs and VRAM
* **Reduce capacity planning variance from ±40% to ±5%**: MIG instances have predictable, fixed resource allocations
* **Simplify quota management**: Assign 2x `2g.10gb` instances per team, enforceable via Kueue (Chapter 4)
* **Enable accurate per-model billing**: DCGM provides per-MIG-instance utilization metrics for cost tracking
* **Predictable failure domains**: OOM or crash in one MIG instance doesn't affect others on same GPU

== Production Considerations and Gotchas

While MIG provides significant benefits, production deployments require careful planning around reconfiguration downtime, workload placement, and ongoing monitoring.

=== Reconfiguration Downtime Planning

Unlike time-slicing (which only requires device plugin pod restart), MIG reconfiguration requires GPU hardware reset and workload migration.

**Downtime Components for Profile Changes**:

1. **Node cordon**: Immediate (prevents new scheduling)
2. **Node drain**: 5-10 minutes (waiting for workload migration to other nodes)
3. **MIG mode enablement**: 10-15 seconds (GPU hardware reset)
4. **Profile application**: 30-60 seconds (instance creation)
5. **GFD label update**: 30-60 seconds (capability rescan)
6. **Device plugin restart**: 30-60 seconds (resource rediscovery)
7. **Node uncordon + scheduling**: 30-60 seconds

**Total**: 10-20 minutes per node for profile changes (per OpenShift documentation)

[WARNING]
====
**Plan MIG Profile Changes During Maintenance Windows**

Changing MIG profiles on production nodes serving live traffic causes:

* Immediate termination of all GPU workloads on that node
* Pod eviction and rescheduling to other nodes
* Temporary capacity reduction during reconfiguration
* Potential cascading failures if spare capacity insufficient

**For rolling MIG updates across N nodes**:

* Plan for **N × 20 minutes** total reconfiguration time
* Ensure **spare GPU capacity** exists for pod rescheduling (recommend N+2 redundancy)
* Use node cordoning to control blast radius: `oc adm cordon worker-gpu-0`
* Test profile changes in dev environment first
* Schedule during low-traffic windows (e.g., weekend maintenance)
* Document rollback procedure (revert to `all-disabled`, then previous profile)
====

=== Workload Placement Strategies

MIG instances require **explicit resource requests**. Workloads must request specific MIG profiles when using **mixed** advertisement strategy.

**Correct Resource Request (Mixed Strategy)**:

[source,yaml]
----
resources:
  limits:
    nvidia.com/mig-2g.10gb: 1  # <1>
----
<1> Request specific MIG profile, matches advertised resource name

**Common Mistake**:

[source,yaml]
----
resources:
  limits:
    nvidia.com/gpu: 1  # <1>
----
<1> ❌ Will NOT schedule on MIG-partitioned nodes using mixed strategy

**Consequences of incorrect requests**:

* Pod stuck in `Pending` state with `FailedScheduling` event
* Error: "Insufficient nvidia.com/gpu" (even though MIG instances available)
* Requires pod specification update and redeployment

[TIP]
====
**Use Admission Controllers for Default MIG Profiles**

For platforms deploying many inference services, create a `MutatingWebhookConfiguration` that automatically injects appropriate MIG resource requests based on pod annotations or namespace labels. This prevents scheduling failures from incorrect resource requests.

**Example implementation**:

* Pods in namespace `small-models` automatically get `nvidia.com/mig-1g.5gb: 1` injected
* Pods with annotation `mig-profile: medium` get `nvidia.com/mig-2g.10gb: 1`
* Pods without annotations remain unchanged (for flexibility)

This pattern reduces operational toil and prevents common scheduling errors, especially useful for teams with 50+ inference services.
====

=== Monitoring MIG Utilization

DCGM (Data Center GPU Manager, deployed in Chapter 1) provides per-MIG-instance metrics. Monitor these to validate your profile choices and identify optimization opportunities.

**Key Metrics to Track**:

* `DCGM_FI_PROF_GR_ENGINE_ACTIVE`: Compute utilization percentage per MIG instance
* `DCGM_FI_DEV_FB_USED`: Memory usage in bytes per MIG instance
* `DCGM_FI_DEV_GPU_TEMP`: Temperature per MIG instance (thermal throttling indicator)
* `DCGM_FI_DEV_POWER_USAGE`: Power consumption per instance

**Red Flags Indicating Misconfigurations**:

* **MIG instance averaging >85% memory usage**: Profile too small, workload may OOM, resize to larger profile
* **MIG instance averaging <20% compute utilization**: Profile too large, wasted resources, resize to smaller profile
* **Frequent pod evictions (OOMKilled)**: Memory oversubscription, increase profile memory allocation
* **High scheduling failure rate**: Insufficient MIG capacity for demand, add nodes or adjust profiles

**Example Prometheus query for MIG memory usage**:

[source,promql]
----
DCGM_FI_DEV_FB_USED{GPU_I_ID=~".*"} /
  DCGM_FI_DEV_FB_TOTAL{GPU_I_ID=~".*"} * 100
----

You'll build comprehensive MIG monitoring dashboards in Chapter 3.

[IMPORTANT]
====
**MIG Instance Naming in Metrics**

DCGM metrics use `GPU_I_ID` label to distinguish MIG instances:

* `GPU=0, GPU_I_ID=0`: First MIG instance on GPU 0
* `GPU=0, GPU_I_ID=1`: Second MIG instance on GPU 0

This differs from Kubernetes resource names (`nvidia.com/mig-2g.10gb`). Correlation requires mapping GFD labels (showing profile types) to DCGM metrics (showing instance IDs).

**Dashboard design tip**: Group metrics by profile type using GFD label joins to show "all 2g.10gb instances" rather than raw instance IDs.
====

== Hardware Requirements and GPU Model Support

MIG is available exclusively on NVIDIA Ampere (A-series) and Hopper (H-series) architecture GPUs:

**Supported GPU Models**:

* **NVIDIA A30 (24GB)**: Ampere architecture, up to 4 MIG instances (profiles: 1g.6gb, 2g.12gb, 4g.24gb)
* **NVIDIA A100-40GB**: Ampere architecture, up to 7 MIG instances (profiles: 1g.5gb through 7g.40gb)
* **NVIDIA A100-80GB**: Ampere architecture, up to 7 MIG instances (profiles: 1g.10gb through 7g.80gb)
* **NVIDIA H100 (80GB/94GB)**: Hopper architecture, enhanced MIG with up to 7 instances plus confidential computing per-instance support

**Unsupported GPU Models** (must use time-slicing instead):

* NVIDIA V100 (Volta architecture)
* NVIDIA T4 (Turing architecture)
* NVIDIA A10, A16, A40 (Ampere, but consumer/workstation GPUs without MIG feature)
* NVIDIA L4, L40 (Ada Lovelace architecture)

[NOTE]
====
**H100 Enhancements Over A100**

H100 GPUs offer additional MIG capabilities beyond A100:

* **Confidential computing support per MIG instance**: Each instance can run in secure encrypted mode
* **Improved memory bandwidth allocation**: Better isolation between instances
* **Faster reconfiguration**: H100 MIG mode changes complete ~15% faster than A100

For most Models-as-a-Service inference workloads, **A100 provides optimal cost/performance balance**. H100 advantages primarily benefit specialized secure computation or extremely memory-bandwidth-intensive workloads. Evaluate H100 premium pricing (~2.5x A100 cost) against actual workload requirements before procurement.
====

== What's Next

In the next lab section, you will apply the concepts from this overview hands-on, transforming your GPU infrastructure from exclusive allocation to multi-tenant MIG-partitioned platform.

**Lab Activities**:

* **Verify MIG capability** on your A100 GPU nodes using `nvidia-smi`
* **Set MIG advertisement strategy** (mixed) via ClusterPolicy patch
* **Label GPU nodes** with built-in MIG profiles (`all-2g.10gb`, `all-balanced`)
* **Monitor MIG Manager logs** during reconfiguration to observe the 10-20 minute workflow
* **Verify MIG instance allocation** via `oc describe node` showing `nvidia.com/mig-*` resources
* **Create custom mig-parted ConfigMap** for heterogeneous profile combinations (e.g., 1x `3g.20gb` + 2x `2g.10gb`)
* **Deploy test CUDA workloads** requesting specific MIG profiles
* **Validate hardware isolation** by running `nvidia-smi` inside pods to confirm dedicated resource allocation

**Expected Outcomes**:

* Single A100-40GB will transform from `nvidia.com/gpu: 1` to `nvidia.com/mig-2g.10gb: 3` allocatable resources
* You'll deploy 3 concurrent CUDA test pods on one physical GPU, each in isolated MIG instances
* You'll observe <1% latency variance between pods running simultaneously (demonstrating isolation)
* You'll create the mixed-inference profile from the multi-tenant scenario (1x `3g.20gb` + 2x `2g.10gb`)
* You'll experience the full MIG reconfiguration workflow including node drain, profile application, and resource verification

**Skills You'll Develop**:

* Label-driven MIG configuration workflow
* Troubleshooting MIG Manager using logs and node labels
* Validating MIG instance creation with `nvidia-smi mig -lgi`
* Correlating Kubernetes resource advertisements with physical MIG partitions
* Planning MIG profile changes with minimal production disruption

This lab transforms your GPU infrastructure from **1 workload per GPU** (full allocation) to **3-7 workloads per GPU** (MIG partitioning) while maintaining production-grade isolation and predictable performance.

////
**Maximizing GPU ROI:**
Understand how the Multi-Instance GPU (MIG) feature splits hardware resources into multiple GPU instances, operating completely isolated from each other. Evaluate MIG advertisement strategies: Single (homogeneous) vs. Mixed (heterogeneous) slicing.
////
