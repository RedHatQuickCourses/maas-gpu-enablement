* xref:ch2-platform-admin-01.adoc[2: MaaS Access Control & Governance]
** xref:ch2-platform-admin-02.adoc[2.1: Subscription-based governance]
** xref:ch2-platform-admin-03.adoc[2.2: Creating OpenShift groups]
** xref:ch2-platform-admin-04.adoc[2.3: Subscriptions and quotas]
** xref:ch2-platform-admin-05.adoc[2.4: Authorizing Gateway Access (MaaSAuthPolicy)]
** xref:ch2-platform-admin-06.adoc[2.5: Generating API Keys]
** xref:ch2-platform-admin-summary.adoc[2.6: Summary and troubleshooting]



////
oc patch datasciencecluster default-dsc --type='merge' -p '{"spec":{"components":{"llamastackoperator":{"managementState":"Managed"}}}}'

enable llamastack operator

cat <<EOF | oc apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: maas-postgres
  namespace: redhat-ods-applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: maas-postgres
  template:
    metadata:
      labels:
        app: maas-postgres
    spec:
      containers:
      - name: postgres
        image: registry.redhat.io/rhel9/postgresql-15:latest
        env:
        - name: POSTGRESQL_USER
          value: "maasuser"
        - name: POSTGRESQL_PASSWORD
          value: "maaspass"
        - name: POSTGRESQL_DATABASE
          value: "maasdb"
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: maas-postgres
  namespace: redhat-ods-applications
spec:
  selector:
    app: maas-postgres
  ports:
  - port: 5432
    targetPort: 5432
EOF


 ////