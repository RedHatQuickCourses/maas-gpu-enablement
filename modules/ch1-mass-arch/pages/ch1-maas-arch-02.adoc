= 1.1: Understanding the GPU silo crisis

ChocoTech Industries has four teams independently managing GPU infrastructure. Let's examine the operational pain points this creates.

The current state of GPU allocations across ChocoTech's business units:

* **Quality Control**: 2x NVIDIA A100 GPUs running defect detection models
* **R&D**: 4x NVIDIA L40S GPUs for recipe optimization
* **Marketing**: 2x NVIDIA L4 GPUs for sentiment analysis
* **Supply Chain**: 2x NVIDIA L40S GPUs for inventory prediction

== The Solution: What is Models as a Service (MaaS)?

Before we dive into the technical architecture, let's define exactly what we are building to solve this crisis. 

The Models-as-a-Service (MaaS) platform acts as a governance layer between users and model serving infrastructure. When users request access to a model, MaaS authenticates the user and determines their assigned tier based on group membership. Policy enforcement checks rate limits and quotas for that tier. 

Authorized requests are forwarded to the appropriate model endpoint, and usage metrics are recorded for tracking and cost allocation purposes. This architecture enables centralized policy enforcement without modifying the underlying model serving infrastructure.

To achieve this, MaaS provides five core capabilities:

* **Tier-based access control:** Define multiple service tiers, such as free, premium, or enterprise, with different rate limits, quotas, and model access permissions for different user groups.
* **Policy and quota management:** Enforce rate limiting and quota policies to prevent resource exhaustion.
* **API key authentication:** Control access to models through tier-based resource allocation and API key-based authentication.
* **Usage tracking:** Track consumption metrics for cost allocation and billing.
* **Observability:** Monitor model access patterns, policy enforcement, and resource utilization.

== The MaaS Impact: Problem vs. Payoff

How does this directly solve ChocoTech's "GPU Silo Crisis"? Review these core challenges and their corresponding MaaS solutions.

[cols="1a,1a",options="header",frame="all",grid="rows"]
|===
| The Pain (The Problem) 
| The Payoff (The MaaS Solution)

| **GPU Under-Utilization**
Quality Control’s $120,000 A100 GPUs sit idle 67% of the time, while R&D waits weeks for capacity. 
| **Dynamic Multi-Tenancy**
Hardware is centralized. MaaS allows dynamic accelerator slicing and multi-tenant sharing, driving up utilization and eliminating wait times.

| **Misallocated Expertise**
Data scientists spend 40% of their time debugging CUDA drivers and Kubernetes manifests. 
| **Simplified Consumption**
Developers skip the infrastructure completely. They access powerful models instantly via secure, OpenAI-compatible API endpoints using simple token authentication.

| **Redundant Deployments**
Four teams independently deploy the same Llama-3 8B model, wasting 128GB of storage and duplicate compute.
| **Centralized Catalog**
A single, highly optimized model deployment serves all business units simultaneously, drastically reducing storage and compute waste.

| **The Security "Black Box"**
Zero visibility into who is sending what data to which model. Audit logs and PII filtering are impossible. 
| **Edge-Level Governance**
Centralized API gateways enforce strict authentication, zero-trust access, and rate limiting directly at the network edge.

| **No Chargeback Visibility**
Finance cannot justify infrastructure costs because they cannot track which teams are consuming compute capacity. 
| **Granular Token Tracking**
Exact consumption metrics (prompt/completion tokens) are tracked per user and tier, enabling precise capacity forecasting and accurate departmental chargebacks.
|===

// TODO: Add diagram showing before/after - siloed teams vs centralized hub
// image::gpu-silo-before-after.png[Comparison of siloed GPU infrastructure vs centralized MaaS model hub,link=self,window=blank,width=800,title="Transforming GPU Infrastructure: From Silos to Centralized Hub"]

== Verify your understanding

Consider these questions based on ChocoTech's situation:

* How would centralized model serving help Quality Control and R&D share GPU capacity?
* What governance policies would prevent one team from monopolizing resources?
* How does token-based consumption enable Finance to perform chargeback?
* What role does the API gateway play in audit logging and PII filtering?